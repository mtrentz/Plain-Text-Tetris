const extraRows = 2;

const settings = {
  // The number of rows in the game board.
  rows: 20 + extraRows,
  // The number of columns in the game board.
  columns: 10,
  // Normal Speed
  normal_speed: 500,
  // Fast Speed
  fast_speed: 50,
};

export default settings;
