const extraRows = 2;

const settings = {
  extraRows: extraRows,
  // The number of rows in the game board.
  rows: 20 + extraRows,
  // The number of columns in the game board.
  columns: 10,
  // Normal Speed
  normal_speed: 400,
  // Fast Speed
  fast_speed: 40,
};

export default settings;
